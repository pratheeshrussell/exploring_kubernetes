apiVersion: v1
kind: Service
metadata:
  name: nginx-node-port
  namespace: ingress-nginx
spec:
  selector:
    app.kubernetes.io/component: controller
    app.kubernetes.io/instance: kube-ingress-nginx
    app.kubernetes.io/name: ingress-nginx
  type: NodePort
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30080